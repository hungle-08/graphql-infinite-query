export type PaginationResponse = {
    pageNumber: number;
    pageSize: number;
    total: number;
    totalPage: number;
  };
  
  export type Pagination = {
    pageNumber: number;
    pageSize: number;
  };
  